<%= form_for(@transaction_category) do |f| %>
  <% if @transaction_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction_category.errors.count, "error") %> prohibited this spending category from being saved:</h2>

      <ul>
      <% @transaction_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-large btn-primary"%>
  </div>
<% end %>
