<h1>Your Transaction Categories</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Is income</th>
      <th>Order in list</th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @transaction_categories.each do |transaction_category| %>
      <tr>
        <td><%= transaction_category.name %></td>
        <td><%= transaction_category.is_income %></td>
        <td><%= transaction_category.order_in_list %></td>
        <td><%= link_to 'Edit', edit_transaction_category_path(transaction_category) %></td>
        <td><%= link_to 'Delete', transaction_category, method: :delete, data: { confirm: "Are you sure you want to delete transaction category '#{transaction_category.name}'?" } %></td>
        <td><%= link_to 'Move Up', "/transaction_categories/#{transaction_category.id}/move_up", method: :post %></td>
        <td><%= link_to 'Move Down', "/transaction_categories/#{transaction_category.id}/move_down", method: :post %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Transaction category', new_transaction_category_path %>
